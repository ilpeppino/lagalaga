# Copy this file to android/keystore.properties and fill in your values.
# This file is intentionally ignored by git and should never be committed.
#
# Example (current Play upload key in this project can be debug.keystore):
# storeFile=app/debug.keystore
# storePassword=android
# keyAlias=androiddebugkey
# keyPassword=android
#
# Example (production upload key):
# storeFile=../lagalaga-upload.jks
# storePassword=your_store_password
# keyAlias=your_alias
# keyPassword=your_key_password

storeFile=
storePassword=
keyAlias=
keyPassword=
