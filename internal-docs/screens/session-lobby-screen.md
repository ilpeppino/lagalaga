# Session Lobby Screen

## Status
Deprecated in the current mobile flow.

The creation journey was consolidated into `/sessions/create` (single-screen squad-builder UX), so users no longer navigate to `/sessions/lobby` immediately after session creation.

## Replacement
See `internal-docs/screens/session-create-screen.md` for the active flow and UI structure.
